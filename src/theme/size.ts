export interface SizeInterface {
  desktop: {
    container: number;
    navbar: number;
  };
}

const size: SizeInterface = {
  desktop: {
    container: 120,
    navbar: 8,
  },
};

export default size;
